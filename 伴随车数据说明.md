# 伴随车数据说明

## 数据格式

[车牌，地点，时间]

- 车牌和地点都经过散列处理，相同的标号即为同一车辆或者同一地点；
- 时间为以秒为单位的Unix时间戳。

> 例如，(132, 1, 200), (198, 1, 2888)分别代表标号为132和198的两辆车，分别在200时刻和2888时刻在标号为1的地点被捕捉到，它们被捕捉到的时间相差2688秒。

## 统计信息

在31天的数据中，记录数约为2.7亿条，不同的车辆数约为2300万，地点数量约为1000。

## 使用建议

可以先降采样，使用较小的数据集（例如1天的数据）进行实验；随着算法的提高再使用更多的数据。同时也可以使用适当的参数，例如提高次数阈值、降低时间阈值来减小候选集，使早期实验更容易进行。

不同车辆出现次数的长尾特征明显，可以对车辆首先进行过滤（例如先取出只出现过1次的车）。